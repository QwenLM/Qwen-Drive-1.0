# Pinned versions of the environment the released weights were verified with
# (Python 3.12, CUDA 12.8). Install with --no-build-isolation so flash-attn
# and causal-conv1d build against the installed torch.
torch==2.8.0
torchvision==0.23.0
transformers==5.14.1
accelerate==1.12.0
safetensors==0.8.0
numpy==2.2.6
pillow==12.0.0
flash-attn==2.8.3
causal-conv1d==1.6.2.post1
flash-linear-attention==0.5.1
opencv-python==5.0.0.93
matplotlib==3.10.7
tqdm==4.67.1
